@model List<digioz.Portal.Domain.DomainModel.Link>

@foreach (var link in Model)
{
    <a href="@link.URL" class="list-group-item">
        <h4 class="list-group-item-heading">@link.Name</h4>
        <p class="list-group-item-text">@Html.Raw(link.Description)</p>
    </a>
}
