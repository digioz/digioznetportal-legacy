@model digioz.Portal.Web.Areas.Forum.ViewModels.ListLatestMembersViewModel

@if(Model.Users.Any())
{
    <ul class="latestmembersjoined">
    @foreach (var user in Model.Users)
    {
        <li><a href="@Html.Raw(user.Value)">@Html.Raw(user.Key)</a></li>
    }
    </ul>
}