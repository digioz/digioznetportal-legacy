@model digioz.Portal.Web.Areas.Forum.ViewModels.ShowVotersViewModel

@if(Model.Votes.Any())
{
    <ul>
        @foreach(var vote in Model.Votes)
        {
            <li>@vote.User.UserName</li>
        }
    </ul>
}