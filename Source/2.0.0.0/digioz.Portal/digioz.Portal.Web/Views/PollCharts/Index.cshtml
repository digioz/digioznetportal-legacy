@model dynamic

@{
    try
    {
        var chart = new Chart(width: 300, height: 300)
            .AddTitle(ViewBag.PollTitle)
            .DataBindTable(dataSource: Model, xField: ViewBag.PollColumn)
            .Write();
    }
    catch (Exception)
    {
        // Ignore
    }
}

